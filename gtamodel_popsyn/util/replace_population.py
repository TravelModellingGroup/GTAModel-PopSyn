import pandas as pd


def replace_populations(population_a: pd.DataFrame, population_b: pd.DataFrame, zones: list):
    """
    Replaces (or inserts) the households and persons records from population_b into population_a.
    This has the effect of firstly removing all records in the passed zone list and repopulating
    those zones with the inserted population.
    @param population_a:
    @param population_b:
    @param zones:
    @return:
    """

    return
